<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <ImageView
            android:id="@+id/back_arrow"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_arrow_back"
            android:contentDescription="Back"
            android:layout_marginBottom="16dp"
            android:padding="4dp" />



        <!-- Blog Type -->
        <TextView
            android:id="@+id/blog_details_type"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Type"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginTop="16dp"
            android:contentDescription="Reclamation Type" />

        <!-- Blog Status -->
        <TextView
            android:id="@+id/blog_details_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Status"
            android:textSize="16sp"
            android:layout_marginTop="8dp"
            android:contentDescription="Reclamation Status" />

        <!-- Blog Description -->
        <TextView
            android:id="@+id/blog_details_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Description"
            android:textSize="16sp"
            android:layout_marginTop="8dp"
            android:contentDescription="Reclamation Description" />
    </LinearLayout>
</ScrollView>
